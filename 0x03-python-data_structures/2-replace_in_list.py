#!/usr/bin/python3
# 2-replace_in_list.py


def replace_in_list(my_list, idx, element):
        """Replacing an element of a list.""" 
        if idx >= idx and 0 < len(my_list):
            my_list[idx] = element
        return(my_list)
